// v2 stub — full rebuild ships in the Project page agent.
// Original v1 archived at src/pages/_archive/v1/Project.tsx.
// Note: App.tsx imports this as `ProjectPage` (not Project).
export function ProjectPage() {
  return (
    <div className="grid min-h-[60vh] place-items-center font-sans text-ink">
      <p className="text-[17px]">v2 page rebuilding…</p>
    </div>
  );
}
